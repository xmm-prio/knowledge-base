## Agent skills

### Issue tracker

Issue 通过本仓库的 `.scratch/` 下的本地 Markdown 文件管理。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的五类 Triage 标签词汇。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用单上下文文档布局。详见 `docs/agents/domain.md`。
