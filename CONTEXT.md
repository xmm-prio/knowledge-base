# 知识库

团队内部的知识库服务：一个文件夹就是一个知识库，里面既有人工维护的文档，也有 agent 沉淀的经验，还有被索引的代码库。组员的 agent 通过 MCP 检索与沉淀，人通过网页浏览与维护。

## Language

### 知识库的组成

**知识库根目录**：
一个文件夹，构成一个完整的知识库；服务在它上面启动，它本身是一个 git 仓库。
_Avoid_: 工作区、仓库、vault

**知识**：
人工维护的、经过沉淀的长效文档，位于 `knowledge/`。agent 只能读，不能写。
_Avoid_: 文档、wiki、笔记

**经验**：
agent 在解决具体问题的过程中产出的、可复用的结论，位于 `learnings/`。agent 可读可写。
_Avoid_: 记忆、教训、心得、learning

**代码库**：
被纳管并索引的源码仓库本体，位于 `codebase/`，由运维手工放入。
_Avoid_: 项目、repo、工程

### 文档的内部结构

**观察**：
文档中一条独立可检索的事实，写作 `- [类别] 内容 #标签`。检索能直接命中单条观察而非整篇文档。
_Avoid_: 事实、条目、observation 之外的译法

**类别**：
观察的分类前缀，开放词汇，例如 `pitfall`、`verified`、`decision`。
_Avoid_: 类型、tag

**关系**：
文档之间的有向连接，写作 `- 关系类型 [[目标文档]]`。
_Avoid_: 链接、引用、边

**摘要**：
一句话概括文档结论，由写入者提供，存在 frontmatter 里，是渐进式披露的第一层。
_Avoid_: 简介、描述、abstract

**语义 Markdown**：
包含观察与关系小节的 Markdown 写法。经验强制使用，知识可选。
_Avoid_: 结构化 Markdown、格式化文档

### 行为

**沉淀**：
把一条经验写入知识库的动作，包含新建、追加、修订三种形态，由同一个入口承担。
_Avoid_: 保存、记录、写入、存档

**修订**：
用新结论覆盖一条已经腐烂的经验；旧结论不保留在正文里，变迁去 git 历史查。
_Avoid_: 更新、编辑、修改

**腐烂**：
一条经验因为代码或环境变化而不再成立的状态。
_Avoid_: 过期、失效、陈旧

**渐进式披露**：
检索先只返回标题、摘要、大纲与命中的观察，需要时再拉全文或某一节的取数方式。
_Avoid_: 分页、懒加载、摘要模式

**内容提纲**：
连接建立时生成的知识库全景速览，随 MCP instructions 一起交给 agent，让它知道这个库里有什么。
_Avoid_: 目录、索引、清单

### 系统角色

**上游**：
被本服务当作黑盒调用、且一行代码都不修改的外部实现，即 basic-memory 与 codebase-memory-mcp。
_Avoid_: 依赖、后端、引擎

**网关**：
本服务对外的唯一门面：一个 Web UI 加一个 MCP 端点，上游的接口一概不外露。
_Avoid_: 代理、中间层、facade
